services:
  mysql:
    image: mysql:8.4
    container_name: saacare-mysql
    restart: unless-stopped
    ports:
      - "3306:3306"
    environment:
      MYSQL_ROOT_PASSWORD: root
      MYSQL_DATABASE: saacare
      MYSQL_USER: saacare
      MYSQL_PASSWORD: saacare
    volumes:
      - saacare_mysql_data:/var/lib/mysql
    healthcheck:
      test: ["CMD", "mysqladmin", "ping", "-h", "127.0.0.1", "-uroot", "-proot"]
      interval: 5s
      timeout: 5s
      retries: 20

volumes:
  saacare_mysql_data:
